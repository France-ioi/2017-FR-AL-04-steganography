import React from 'react';
import EpicComponent from 'epic-component';

export default EpicComponent(self => {

   self.render = function () {
      return (
         <div>
            <p>Find the key that will decrypt all ciphers.</p>
         </div>
      );
   };

});
